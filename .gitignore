# Private agent skills (work-related, may contain sensitive info)
.agents/skills-private/

# Local secrets (webhooks, tokens, etc.) — never tracked
.config/secrets/

# Local planning scratch files
plans/

# macOS Finder metadata
.DS_Store
